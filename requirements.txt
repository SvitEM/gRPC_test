# gRPC Latency Tests - Python Dependencies
# Runtime dependencies (minimal set as per CLAUDE.md specification)

# Core gRPC library - required for both client and server
grpcio>=1.60.0

# Environment configuration support
python-dotenv>=1.0.0

# Protocol buffer runtime - required for generated stubs
protobuf>=4.21.0,<5.0.0

# Build-time dependency for protobuf stub generation
# Install separately: pip install grpcio-tools>=1.60.0